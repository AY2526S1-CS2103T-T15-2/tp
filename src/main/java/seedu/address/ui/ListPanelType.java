package seedu.address.ui;

/**
 * Enumeration for what type of list panel to show to the user.
 */
public enum ListPanelType {
    CONTACT,
    POLICY,
    CONTRACT,
    APPOINTMENT,
    /** Type denoting no change to the currently shown list panel. */
    CURRENT,
}
